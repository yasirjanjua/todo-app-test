"""Application layer: profiles, the wizard state machine, and the closed-loop play controller.

This is where ``core/``, ``vision/``, and ``backends/`` get wired together into the actual
product. UI code (``ui/``) drives this layer; this layer never imports from ``ui/``.
"""
