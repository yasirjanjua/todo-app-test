"""Pure game logic and AI: bitboard representation, move rules, and the expectimax solver.

Nothing in this package touches the filesystem, the screen, or the OS. That purity is what
lets ``solver.py`` be unit-tested against fixed boards and reused by any future frontend.
"""
